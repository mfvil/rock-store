import { StateInterface } from "@/globalTypes";
import React, { useContext } from "react"

const RockDetails = (): JSX.Element => {
    const { products } = useContext<StateInterface>(ctx)
    return(
        
    )
}

export default RockDetails;